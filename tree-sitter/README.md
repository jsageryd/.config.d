# tree-sitter grammars

This directory contains tree-sitter grammars. Run `make` to build parsers for
all of them. The compiled parsers are placed in `parsers/`. This directory is
symlinked from `nvim/parser/`, so the parsers are directly available to Neovim.
